import { QuestionnaireAnswers } from './questionnaire-answers';

describe('QuestionnaireAnswers', () => {
  it('should create an instance', () => {
    expect(new QuestionnaireAnswers()).toBeTruthy();
  });
});
