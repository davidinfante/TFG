/**
 * Information that any page in the header menu must have
 */
export class Page {
  title: string;
  url: string;
  icon: string;
  showIcon: boolean;
  fill: string;
  color: string;
  position: string;
}
